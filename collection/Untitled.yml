info:
  name: Untitled
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:4200/api/google-sheet
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
