info:
  name: Search Construction Demand By Sei Process
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:4200/api/construction-demands/search?process=020.2296.2025.0001737-43
  headers:
    - name: Accept
      value: application/json
  params:
    - name: process
      value: 020.2296.2025.0001737-43
      type: query
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("should return 200", function() {
          expect(res.status).to.equal(200);
        });

        test("should return a successful JSend response", function() {
          expect(res.body.status).to.equal("success");
          expect(res.body.data).to.be.an("object");
        });

        test("should return construction demand process search data", function() {
          expect(res.body.data.term).to.equal(null);
          expect(res.body.data.total).to.be.a("number");
          expect(res.body.data.data).to.be.an("array");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
