info:
  name: Escolher painel de obras
  type: http
  seq: 2
  tags:
    - whatsapp
    - flow
    - panel

http:
  method: POST
  url: "{{base_url}}/api/whatsapp/messages"
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "message": "1",
        "phone": "{{test_phone}}",
        "sender_name": "{{test_sender_name}}",
        "external_id": "bruno-panel-select-{{$randomUUID}}",
        "source": "manual_staging_test"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("should acknowledge the message", function() {
          expect(res.status).to.equal(202);
          expect(res.body.status).to.equal("success");
          expect(res.body.data.accepted).to.equal(true);
        });

settings:
  encodeUrl: false
  timeout: 10000
  followRedirects: true
  maxRedirects: 5

docs: |-
  # Escolher painel de obras

  Envia a opção `1` retornada pelo menu do assistente.
